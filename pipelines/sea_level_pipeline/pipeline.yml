# bruin run pipelines/sea_level_pipeline/pipeline.yml

name: sea_level_pipeline
schedule: "0 2 * * *"

default_connections:
  google_cloud_platform: gcp_climate

environments:
  default:
    python:
      executable: /workspaces/climate-correlation-engine/.venv/bin/python
