# climate-correlation-engine
This project demonstrates an end-to-end batch data engineering pipeline built on Google Cloud Platform (GCP). The pipeline ingests climate datasets, stores them in a data lake, transforms them in a data warehouse, and powers a dashboard for analytics.
